<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission
        android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/vibrant_developer"
        android:roundIcon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/Theme.Week12_App"
        tools:targetApi="31">

        <activity
            android:name=".EditProfile"
            android:exported="false"
            android:label="Edit Your Profile" />
        <activity
            android:name=".UserInfoActivity"
            android:exported="false"
            android:label="User Information" />
        <activity
            android:name=".HomePage"
            android:exported="false"
            android:label="We Love Nerds!!" />
        <activity
            android:name=".SignUp"
            android:exported="false"
            android:label="Sign Up Here" />
        <activity
            android:name=".SignInActivity"
            android:exported="false"
            android:label="@string/login_here" />
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>